# Server Environment Variables - EXAMPLE FILE
# Copy this file to .env and update with your actual values

# MongoDB Connection String
# For local development:
MONGODB_URI=mongodb://localhost:27017/taskboard

# For MongoDB Atlas (production):
# Replace YOUR_USERNAME, YOUR_PASSWORD, and YOUR_CLUSTER with your actual MongoDB Atlas credentials
# MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/taskboard?retryWrites=true&w=majority

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development

# CORS Origins (comma-separated for multiple origins)
# For development:
CORS_ORIGIN=http://localhost:5173

# For production (add your frontend URL):
# CORS_ORIGIN=http://localhost:5173,https://your-frontend-url.com

# JWT Secret Key
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

